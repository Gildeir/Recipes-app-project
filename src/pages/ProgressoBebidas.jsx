import React/* , { useState, useEffect, useContext } */ from 'react';
import PropTypes from 'prop-types';
// import ContextRecipe from '../provider/ContextRecipe';

function ProgressoBebidas(/* props */) {
  // const { match: { params: { id } } } = props;
  // const { ApiIdDetalhe, funcHeartColor, heartColor } = useContext(ContextRecipe);
  // passar ApiIdDetalhe, id como parametros para funcHeartColor no onclick do coração de favoritar
  // apidetalhe é o item que tem que renderizar na tela .
  return (
    <p>olá</p>
  );
}

// ProgressoBebidas.propTypes = {
//   match: PropTypes.objectOf(PropTypes.any).isRequired,
// };

export default ProgressoBebidas;
